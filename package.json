{
  "name": "zookeeper",
  "version": "1.0.0",
  "description": "this is the zookeeper clone that monitors the backend servers and notifies the reverse proxy",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aravind Siddharth R",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
